<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/taskCard"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="0dp"
    android:layout_marginVertical="8dp"
    app:cardCornerRadius="16dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/ui_background"
    app:strokeWidth="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Category -->
        <TextView
            android:id="@+id/tvCategory"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Proofreading &amp; Editing"
            android:textSize="14sp"
            android:textColor="@color/gradient_end"
            android:fontFamily="@font/public_sans_medium"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="@+id/priorityChipContainer"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/priorityChipContainer" />

        <!-- Priority Chip -->
        <LinearLayout
            android:id="@+id/priorityChipContainer"
            android:layout_width="wrap_content"
            android:layout_height="24dp"
            android:orientation="horizontal"
            android:gravity="center"
            android:paddingHorizontal="6dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="@id/tvCategory"
            app:layout_constraintEnd_toEndOf="parent">

            <TextView
                android:id="@+id/tvPriority"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Low"
                android:textSize="11sp"
                android:fontFamily="@font/public_sans_medium"
                android:textColor="@color/priority_Low" />
        </LinearLayout>

        <!-- Task Title -->
        <TextView
            android:id="@+id/tvServiceTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Business Report"
            android:textSize="14sp"
            android:textColor="@color/text_dark"
            android:fontFamily="@font/public_sans_bold"
            android:layout_marginTop="12dp"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintTop_toBottomOf="@id/tvCategory"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/ivChevron" />

        <!-- Chevron Icon -->
        <ImageView
            android:id="@+id/ivChevron"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginTop="6dp"
            android:src="@drawable/ic_chevron_right"
            android:contentDescription="View details"
            app:tint="@color/gradient_end"
            app:layout_constraintTop_toTopOf="@id/tvServiceTitle"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Submitted Date -->
        <TextView
            android:id="@+id/tvSubmittedDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Submitted: 10/10/2025"
            android:textSize="11sp"
            android:textColor="@color/text_faded"
            android:fontFamily="@font/public_sans_medium"
            android:layout_marginTop="8dp"
            app:layout_constraintTop_toBottomOf="@id/tvServiceTitle"
            app:layout_constraintStart_toStartOf="parent" />

        <!-- Status + Deadline Row -->
        <LinearLayout
            android:id="@+id/statusRow"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginTop="12dp"
            app:layout_constraintTop_toBottomOf="@id/tvSubmittedDate"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent">

            <!-- Status Dot -->
            <View
                android:id="@+id/statusDot"
                android:layout_width="12dp"
                android:layout_height="12dp"
                android:background="@drawable/bg_status_dot" />

            <!-- Status Label -->
            <TextView
                android:id="@+id/tvStatusLabel"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:text="Pending"
                android:textSize="11sp"
                android:textColor="@color/text_dark"
                android:fontFamily="@font/public_sans_medium" />

            <!-- Deadline -->
            <TextView
                android:id="@+id/tvDeadline"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Deadline: 28/10/2025"
                android:textSize="11sp"
                android:textColor="@color/text_faded"
                android:fontFamily="@font/public_sans_medium" />
        </LinearLayout>

        <!-- Admin-only Dropdown (hidden by default) -->
        <com.google.android.material.textfield.MaterialAutoCompleteTextView
            android:id="@+id/ddConsultants"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:hint="Assign consultant"
            android:textSize="13sp"
            android:padding="12dp"
            android:background="@drawable/bg_dropdown_outline"
            android:textColor="@color/text_dark"
            android:textColorHint="@color/text_faded"
            android:inputType="none"
            android:focusable="false"
            android:clickable="true"
            android:visibility="gone"
            app:layout_constraintTop_toBottomOf="@id/statusRow"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
